program
{
	int x = 10, y;
	y = 10;

	do
	{
		x = x + 2;
		y = y - 1;
	}
    while ((x <= 30) or (y > 5));
	writeline("x = ", x);
	writeline("y = ", y);
	writeline(" ");

	do
	{
		x = x - 2;
		y = y + 1;
	}
    while ((x >= 5) and (y < 20));
	writeline("x = ", x);
	writeline("y = ", y);
}